	44. 의존관계 자동 주입 - @Autowired 필드 명,  @Qualifire, @Primary
	
		- @Autowired : 타입이 동일한 빈이 2개 이상 있으면, 필드명을 매칭해서 가져온다.
			1. 타입 매칭해서	
			2. 타입 매칭의 결과가 2개 이상일 때 파라미터 명(필드 명)으로 빈 이름 매칭
			
		- @Qualifire : 추가 구분자를 붙어주는 방법, 주입시 추가적인 방법을 제공하는 것이지 빈 이름을 변경하는 것은 아니다.
		
		- @Primary : 우선순위를 정하는 방법이다. @Autowired시 여러 빈이 매칭되면 @Primary가 우선권을 가진다.
		
			
			